{
  "name": "@tutorfin/ledgertext",
  "version": "0.0.0",
  "private": true,
  "description": "TutorFin LedgerText abstraction (TS). Beancount canonical; pluggable adapters.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "beancount-parser": "^0.1.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.5"
  }
}
